const express = require("express")
const nodemailer = require('nodemailer');
const sendEmailRouter = express.Router();

sendEmailRouter.route('/').post((req, res) => {

    

})
module.exports = sendEmailRouter;

